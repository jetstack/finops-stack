{{ if .Values.grafana.route.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana-route
  labels:
    {{- include "ingress-config.labels" $ | nindent 4}}  
spec:
  parentRefs:
  - name: {{ .Values.grafana.route.gatewayName }}
    namespace: {{ .Values.grafana.route.gatewayNamespace }}
  hostnames:
  - {{ .Values.grafana.route.hostname }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: grafana
      port: 80
---
apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: grafana-route
spec:
  default:
    checkIntervalSec: 5
    healthyThreshold: 1
    unhealthyThreshold: 2
    logConfig:
      enabled: true
    config:
      type: HTTP
      httpHealthCheck:
        port: 3000
        requestPath: /api/health
  targetRef:
    group: ""
    kind: Service
    name: grafana   
{{ end }}